# Data Notes

The source workbook for this project is `shin_clinical_data.xlsx` at the project root.

This folder can be used later for derived, non-sensitive intermediate artifacts if needed. For this homework submission, the original workbook remains in place to avoid unnecessary duplication.
